export default function EmailVerificationPage () {
    // Get Route queries

    // Pass route queries as params to verify api
    // Loading state while verifying

    // Display errors if any

    // Message success & timeout 5s => redirect to Home

    const renderContent = () => {
        // switch (true) {
        //     case error:
        //         return ()
        //         // display errors + support links
        //     case success:
        //         //display success + redirectlink & countdonw before redirecting
        //         return ()
        //     case loading:
        //     default:
        //         return <Spinner/>
        // }
    }

    return (
        <div className="w-screen h-screen">
            {/* {renderContent()} */}
        </div>
    )
}